---
// Redirect /about to /en/about (default language)
const getUrlWithBase = (href: string) => {
    const isDev = import.meta.env.NODE_ENV !== 'production';
    const basePath = ''; // No base path for Vercel
    
    return href !== undefined && href !== null ? `${basePath}/en/${href}` : href;
};
return Astro.redirect(getUrlWithBase(''), 301);
---
